<a href="{{ link }}" class="TimelineEntry">
	<img class="EntryImage" src="{{ articleImage }}">
	<div class="EntryText">
		<h1 class="EntryTitle">{{ title }}</h1>
		<h2 class="EntryDate">{{ date }}</h2>
		<p class="EntryDescription">{{ articleDescription }}</p>
	</div>
</a>
